// import getAvailableProductsList from './getAvailableProductsList';
// import * as dataStorage from '../../dataStorage';
// import * as utils from '../../utils';

describe('getAvailableProductsList', () => {
  it('', () => {});
  //   it('should return list of available products', async () => {
  //     const expectedDataFromStorage = await dataStorage.getAvailableProducts();
  //     const expectedResponse = utils.createResponse(200, expectedDataFromStorage);

  //     const spyGetAvailableProducts = jest.spyOn(dataStorage, 'getAvailableProducts');
  //     const spyCreateResponse = jest.spyOn(utils, 'createResponse');

  //     const response = await getAvailableProductsList();

  //     expect(response).toEqual(expectedResponse);

  //     expect(spyGetAvailableProducts).toHaveBeenCalled();
  //     expect(spyCreateResponse).toHaveBeenCalledWith(200, expectedDataFromStorage);
  //   });
});
